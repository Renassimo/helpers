export const redirectToSignIn = {
  redirect: {
    permanent: false,
    destination: '/signIn',
  },
};

export const redirectToMain = {
  redirect: {
    permanent: false,
    destination: '/',
  },
};

export const showNotFound = { notFound: true };
